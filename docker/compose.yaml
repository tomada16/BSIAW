services:
  web:
    build:
      context: ../
      dockerfile: docker/web/dockerfile
    networks:
      netlink: {}
      dblink: {}
    ports:
      - "7708:80"
    volumes:
      - ../web:/srv/web
    environment:
      DB_CREDENTIALS: "{\"username\":\"postgres\",\"password\":\"postgres\"}"

  db:
    build:
      context: ../
      dockerfile: docker/db/dockerfile
    networks:
      dblink: {}

networks:
  netlink:
  dblink:
    driver: bridge
