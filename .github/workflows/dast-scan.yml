name: DAST Scan

on: [push, workflow_dispatch]

jobs:
  security_scan:
    runs-on: ubuntu-latest
    name: OWASP ZAP Scan

    steps:
      # checkout code
      - name: Checkout
        uses: actions/checkout@v4

      # verify context file presence
      - name: DEBUG - Check Context File
        run: |
          echo "=== LISTING ==="
          ls -la
          echo "=== CONTENT OF zap.context ==="
          cat zap.context || echo "FILE MISSING!"

      # start docker containers from makefile logic
      - name: Start Environment
        run: |
          docker compose -f docker/compose.yaml up -d --build
          echo "waiting for services to start..."
          sleep 60

      # run owasp zap full scan
      # target localhost port 7708
      # enable ajax spider for js handling
      # load authentication context and user alice
      # ignore self-signed cert errors
      # do not fail build on findings
      - name: ZAP Scan
        uses: zaproxy/action-full-scan@v0.10.0
        continue-on-error: true
        with:
          target: 'https://localhost:7708'
          cmd_options: '-a -I -j -n /zap/wrk/zap.context -U Alice'
          allow_issue_writing: false
          fail_action: false

      # upload reports manually to fix 400 bad request error
      # overwrite existing artifacts if needed
      - name: Upload ZAP Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap_scan_reports
          path: |
            report_json.json
            report_md.md
            report_html.html
          overwrite: true
